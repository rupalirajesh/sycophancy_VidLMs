# ── training ───────────────────────────────────────────────────────────────────
# Install PyTorch separately: https://pytorch.org/get-started/locally/
transformers>=4.57.0
peft>=0.18.0
accelerate>=0.34.0
qwen-vl-utils
wandb>=0.17.0

# ── dataset generation (build_sycophancy_dpo.py / download_videos.py) ──────────
openai>=1.30.0
huggingface_hub>=0.24.0
tqdm>=4.66.0

# ── benchmark generation (build_sycobench.py) ───────────────────────────────────
# google-generativeai is only needed for Tier 2 VLM oracle (Gemini watching videos).
# Phase 2 text generation uses the OpenAI-compatible endpoint — no extra install needed.
google-generativeai>=0.8.0

# ── utilities ──────────────────────────────────────────────────────────────────
matplotlib>=3.7.0
